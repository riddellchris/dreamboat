<?php
if(!isset($_SESSION)){session_start();}


//this should probably be a three column layout but I can't quite think of what that should be right now...
//so... let's just make it a two column setup
require $_SERVER['DOCUMENT_ROOT']."/components/layouts/two_column_layout/display.php";

?>