<?php
if(!isset($_SESSION)){session_start();}

require $_SERVER['DOCUMENT_ROOT']."/financials/holding_page.php";

/*
simple - very simple plots and then a data table of revenue and expenses over the past 24 months
potentially go to all time as a seperate sub menu to copy it out / view it properly


so obviously for nigel 

and hours in general 

so the first thing to do and understand here is just how many budgets exist
and ideally if so:
    when the are for
    when they 
        GEThttps://api.xero.com/api.xro/2.0/Budgets
        //so let's do that for one
        //then i can pull that out for someone else 
        //then i can pull that through
        //so first let's test that for leigh honestly


        //then we can present that
require $_SERVER['DOCUMENT_ROOT']."/components/layouts/standard_page/start.php";
require $_SERVER['DOCUMENT_ROOT']."/components/layouts/standard_page/end.php";
*/



//