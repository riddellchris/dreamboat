//CR 07.01.21
/*
NOw what becomes apparent is that there isn't a company entity but perhaps there should be to make all those edges count - but I guess that just goes in inishgts
the matching should be though for client to contact in xero for example really shouldn't it so that's how that goes

There are actually two elements to this matching folder which has to be 
1. Matching contacts to each one of the API inputs such that they are all known and defined
2. Matching those contacts and connections across multiple users within a business and ultimlately an economy.
  -- for this one in particular we need to note that the user
Really it isn't impossible but important none the less.

so where do i begin here though?
I think clearly the first thing to do is pull out all the contacts from Xero and go that way do let's start there.

the biggest issue here is that this should really be done with just well it should really be part of a submenu shouldn't it not anything else so let's do that first and foremost so let's make that change 
*/

//so first it seems that what i need to do is start with some sort of prioritisation matrix which can probably be the restul or the outcome of the appropriate 


//so to start this should probably search all the connected systems from the table api_connections because from there we can sort out what connections are required
//perhaps this needs to be a worksheet/ table of all the connected applications 
//match status style
  ///xero  contacts // up to date/needs work/nothing completed as yet /// then work from there
  ///xero  accounts //
//this way does seems to make most sense as this means that as data gets updated we can continue forward and keep things up date.
//Most importantly it means that we can really start to "extract" the data from systems and ADD more data into our system as a result, extracting what we need to 
//AND even more importantly getting total control over the things like invoices and sales and triggering loads of alerts from there which is just so freaking exciting
//this means all that hard work will actually come together now

//SO back to this page we are going to have the status updates from every API
//then we are going to have SOME things that we know and understand what they match to and then we will also have some that quite frankly we just don't know exactly what they are.
  //MOST contacts for example should be determinable based on the ACCOUNT that they occur in as part of the Xero
    //but regardless of that and this there will be some that we need to flag.
    //I guess that can just be something that we go over with every user in time
      //further to that we could check notes, we could do any other number of things as required for sure
      //such as get predictions from other users about the same name
      //i think the worst will be the strange payments from trust accounts that will be super difficult to account for though, that's probably the worst to account for sadly.
  //With that said I suspect that most things will be pretty obvious.


//so back to the functionality of this page specifically
//what I need to work on is the layout which for my mind is quite simply just going to be A table.
//probably most simply just with the same number of columns as with the matching table itself (other than the tracking ones)

//then once this is done i can make those charts on the network pages and boom even cooler yet again.
//snapity snap
//i can probably even pull in budgeting into the systems as well! holy cow cow