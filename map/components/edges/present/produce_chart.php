<?php
if(!isset($_SESSION)){session_start();}

//the intent of this edge is to ensure that we produce a chart per edge
//in time this will most likely be cut off to be as you please or integrated into the edge themselves as opposed to simply on an edge

//$new_translate_y = translateY(".$edges['item_a_y_pos'][$i]."px)
//original translate
// + the line thickness
// + say 2 jsut to make that stick with a nice little border
//then replace in the style string.
// this could be done with the sytle string or just build that string slightly differently for both uses
//other than combinbing multiple google charts on the same page just like
//  /knowledgebase/economy/fundamentals/ there is nothing wrong with this at all

/*
echo "<a>";
echo "<div style='position:absolute;".$style_string."'>";
 echo "hhhhhhhhhhhhhhhhhh";
echo "</div>";
echo "</a>";
*/