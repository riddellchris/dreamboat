The next iteration of this system is most likely going to be that there are a few critical changes to the way that this functions.

The deepest change would be that there would be views that would start with just "everything"/ default as the beggining views
and then from there both pilots and users would be able to change / add to this.
-- I suspect that this would mean this views "select" would be at the top of the menu perhaps with an "edit" buttion "manage" button next to it such that more views can be:
    --- added
    --- removed
    --- renamed as appropriate

Rather than the slightly / overly complicated version of the multiple "selects" that really should just simplify into a straight forward "checkbox" scenario where nothing else mattered
//I guess the biggest issue is more around the requirement for
  // what happens when all checkboxes are not there

  //what would be most interesting about this and frankly most challenging is that it is "pinned" on the concept of zooming in and out ideally such that rather than having to position and reposition into the same place each time the same connections 
  //this way the views pretty much remain but we can adjust all these things accordingly.

  //perhaps what this would mean is that as you change views most things stay proportional so in actual fact the co-ordinates actually become a proportional function per view depening on what exists / is being displayed.
    // this may be more complicated/far more complicated that I care to admit but that's where this should ultimately go.


//the principle purpose of this though (more accurately - the main reason behind this ) is to simplify the mapping process.
//I guess though/ that said, my main driving motivation is to find the simplest way to find / create a critical map of problems for myself but also for everyone / each user / collectively etc etc etc.

The biggest hurdles in successfully completing this are:
1. Starting to make the "Zoom" in and out work which would/will require
  -- changing the size of the background checks to facilitate / enable & communicate that there are some changes occurring
  -- enabling the zoom buttons to work
  -- dropping off / out the "unselected" nodes such that only the selected ones appear and in such a format that they are still visible but in the same proportional arrangment as they would have been previously.
        //none of this is impossible by any stretch of the imagination but it is going to be challenging to build this in such a way that it all works in a timely fashion.

        //the most important reason to do this is to ensure 
    //drag and drop would also be more than ideal rather than anything else confusing
    //why not just use lucid chart?
    i'm not entirely sure other than the connections are so critical to document in our own database such that we can actually create and optimse the entire system.