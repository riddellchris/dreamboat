<?php
if(!isset($_SESSION)){session_start();}

//we probably need all of this change to occur via one central file such that we can adjust accordingly.
//perhaps though it's just a case of displaying an "error" or select as appropriate on each of those charts as we go now.
//for example a 2 month average if less than that - will cut out so probably should show red etc etc 
//nice enough