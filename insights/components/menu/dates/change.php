<?php
if(!isset($_SESSION)){session_start();}



//obviously this then needs to be translated into the appropriate queries for each api endpoint
//pretty much though that'sall good and mechanical now


//so really this should be done off the $_GET['queries']
//and then we can rip that out into other things