<?php
if(!isset($_SESSION)){session_start();}

//set defaults



//then run and test for every user as more and more types of defaults apply such that all we need to do when adding a new control measure is load this file.
//i.e. 

// for every user id that's currently active
//check if there is latest version of every default control // if not then set it as required

//if undelete a user on the very odd occasion probably need to run this.

//perhaps even run this on every sign in as well for each user or on select client...
//therefore allowing as much speed and alteration of change as possible.